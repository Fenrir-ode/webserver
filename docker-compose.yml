version: "3.9"
services:
  nginx:
    image: nginx
    volumes:
    - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
      
  server:
    expose: [3000]
    container_name: server
    build: .
    ports:
      - "3000:3000"